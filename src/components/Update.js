import React from 'react'

export default function Update(props) {

  

  return (
      <form>
        <input name='updates' placeholder='enter updates'></input>
        <button>Save</button>
      </form>
  )
}
